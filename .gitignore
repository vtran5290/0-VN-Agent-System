# Local env with API keys — never commit
.env
.env.local
*.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Excel / Office lock files (never commit)
~$*

# Data cache (optional: remove if you want to track cache)
data/cache/
# FireAnt OHLCV bulk cache (regenerate; large)
data/research/cache/

# Ad-hoc run logs
exit_research_run.log

# Large research zip (use v2 handover package or regenerate)
data/research/ema_cloud_research_package.zip

# Heavy generated data — regenerate via scripts
data/fireant_exports/
minervini_backtest/outputs/
pp_backtest/feature_log_ema21_pp_weekly.csv
pp_backtest/monthly_universe_eligibility.csv
pp_backtest/pp_portfolio_signal_log.csv
artifacts/*.html
artifacts/*.csv
