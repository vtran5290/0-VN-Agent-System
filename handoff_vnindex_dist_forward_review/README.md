# VNINDEX distribution / forward-return — handoff bundle

1. Open **`PROMPT_FOR_REVIEWER.md`** (full instructions for the reviewing AI).  
2. Read **`MANIFEST.md`** (paths, dependencies, run commands).  
3. Compare code output to **`RESULTS_BASELINE.json`** when validating.

Archive of this folder: **`vnindex_dist_methodology_handoff_2026-05-14.zip`** (one level up in the parent repo if created there).
