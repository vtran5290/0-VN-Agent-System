# M4B — Volume either (thrust OR dryup), close-break
name: M4B_vol_either
description: "TT Lite + VCP; Close > pivot; vol_mode either (thrust or dryup); retest"

tt: lite
ma200_slope_bars: 20
setup: vcp

lookback_base: 40
breakout_mode: close
vol_mode: either
vol_mult: 1.2
close_strength: true

use_retest: true
retest_max_bars: 5
max_undercut_pct: 0.02

stop_pct: 0.05
atr_k: 2.0
risk_pct: 0.006

exits:
  hard_stop: true
  trend_break_ma: 50
  fail_fast_days: 3
  time_stop_days: 0
  take_partial_r: null
  trail_ma: null
  climax_proxy: false

fee_bps: 20
slippage_bps: 5
min_hold_bars: 0
