# M6 — Breakout + No-Chase rule (reduce buying extended)
# Entry only if Close <= pivot * (1 + chase_cap), e.g. 1.5%
name: M6
description: "M1-style + No-Chase: Close <= pivot*(1+1.5%); VN often gaps up then fades"

tt: lite
ma200_slope_bars: 20
setup: vcp
lookback_base: 40
vol_mult: 1.5
close_strength: true
chase_cap: 0.015

stop_pct: 0.05
atr_k: 2.0
risk_pct: 0.0075
stop_vol_only: false

exits:
  hard_stop: true
  trend_break_ma: 50
  fail_fast_days: 0
  time_stop_days: 0
  take_partial_r: null
  trail_ma: null
  climax_proxy: false

fee_bps: 20
slippage_bps: 5
min_hold_bars: 0
use_retest: false
