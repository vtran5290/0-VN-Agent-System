[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --import-mode=importlib
# Keep repo root importable when collecting from multiple dirs (e.g. minervini_backtest/tests).
pythonpath = .

